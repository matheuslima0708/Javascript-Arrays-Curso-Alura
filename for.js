const numeros = [100, 200, 300, 400, 500, 600, 700];

// Primeira expressao : e executada apenas uma unica vez
// segunda expressao: condicao de execucao
// terceira expressao: e executada sempre sao final do bloco

for (let indice = 0; indice < numeros.length ; indice++) {
    console.log(numeros[indice]);
}